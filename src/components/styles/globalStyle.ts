import { css } from "@emotion/react";
import { FONT_FAMILY, GrayColors } from "./const";

export const globalStyle = css`
  body: {
    font-family: ${FONT_FAMILY};
    /* background: ${GrayColors.GRAY_50}; */
    /* background: "#333"; */
    /* color: ${GrayColors.GRAY_800}; */
    margin: 0;
  }
`;
